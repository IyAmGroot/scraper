<div class="container-fluid">
    <div class="row topRow justify-content-center">
        <div class="md-col-12">
            <div class="jumbotron">
                <h1 class="display-4">Scrape This!</h1>
                <p class="lead">This site allows you to scrape the New York Times website. Scrapping will save new
                    articles into our database and save.</p>
                <hr class="my-4">
                <p>You may add comments to the articles that are scraped.
                </p>
                <p class="lead">
                    <a href="/scrape"><button id="scrapeBtn" class="btn btn-primary btn-lg">Click to Scrape</button></a>
                </p>
            </div>
        </div>

    </div>

</div>
<div class="container">
    <div>
        <div class="row-fluid">
            <div>
                <div class="col-md-12">
                    <hr>
                    {{#each articles}}
                    <div class="row ">
                        <div class="col-sm-2">
                            <span><img src={{imageUrl}} alt={{imageURL}} class="img-responsive myImage"
                                    style="max-height: 100px"></span>
                        </div>
                        <div class="col-sm-8">
                            <div class="row">
                                <div class="col-sm-12">
                                    <h4><a href={{articleUrl}}>{{title}}</a></h4>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-sm-12">
                                    <p>{{summary}}</p>
                                </div>
                            </div>
                        </div>
                        <div class="col-sm-2">
                            <button class="btn btn-primary btnComment" data-id={{id}}>Add/Edit Comment</button>
                        </div>
                    </div>
                    <hr>
                    {{/each}}

                </div>
            </div>
        </div>
    </div>

</div>